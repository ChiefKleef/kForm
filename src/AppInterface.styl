.appInterface {
  position: absolute;
  z-index: 100;
  width: 100vw;
  height: 100vh;
  pointer-events: none;
  color: white;
}

.form-container {
  position: absolute;
  width: 100%;
  height: 100%;
}